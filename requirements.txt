# Core Engine
fastapi>=0.100.0
uvicorn>=0.20.0
websockets>=12.0

# UI Framework: React UI (see react-ui/ folder)
# No Python UI dependencies needed

# Utilities
networkx>=3.0
python-dotenv>=1.0.0
loguru>=0.7.0

# AI Integration
google-generativeai>=0.3.0
pydantic>=2.0.0
underthesea>=1.3.0  # Vietnamese tokenization (optional)

# Redis for session state (optional but recommended for multi-worker)
redis>=4.6.0

# Rate limiting
slowapi>=0.1.9

# Security
cryptography>=41.0.0  # For API key management

# Development Tools
pytest>=7.0.0

# Optional: Legacy RAG System (not used, kept for backward compatibility)
# sentence-transformers>=2.2.0
# chromadb>=0.4.0
# scikit-learn>=1.3.0
# numpy>=1.24.0
